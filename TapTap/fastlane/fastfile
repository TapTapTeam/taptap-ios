# Nbs/fastlane/Fastfile

default_platform(:ios)

platform :ios do
  desc "Push a new beta build to TestFlight"
  lane :beta do
    build_app(
      workspace: "NBS.xcworkspace",
      scheme: "Nbs"
    )
    upload_to_testflight
  end
end
